# Roadmap facts

- Active roadmap index: `docs/dev/roadmap.md`.
- Use short kebab-case item IDs with statuses `Pending`, `Partial`, or `Blocked`.
- Detailed active scope may live in `docs/dev/roadmap-items/`; plans live in `docs/dev/plans/` with zero-padded numeric prefixes.
- Completed work moves into specs, decisions, README, release governance, and changelog; remove completed items from the active index.
- Keep only executable future slices in the active sequence.
