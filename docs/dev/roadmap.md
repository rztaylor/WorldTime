# Product roadmap

The initial usable release described by `docs/PRODUCT.md` is implemented as the repository foundation.

## Active sequence

1. `expanded-location-catalog` — **Pending** — broaden curated city and country coverage while preserving bundle size and search quality.
2. `future-time-preview` — **Pending** — add the planned shared display timestamp scrubber and date-boundary previews.
3. `shareable-comparisons` — **Pending** — encode selected IANA zones in a privacy-preserving URL.
