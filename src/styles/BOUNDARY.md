# Styles boundary

Owns global design tokens, document defaults, shared interaction states, page composition, and responsive rules for the approved monochrome interface.

Does not own component content, business behavior, or geographic data.

Components may consume these stable class contracts. Repeated colors, radii, shadows, and typography values belong here rather than in feature-local literals.
