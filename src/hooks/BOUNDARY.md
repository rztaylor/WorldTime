# Hooks boundary

Owns small reusable React lifecycle hooks shared by product features.

Does not own domain state, persistence schemas, feature markup, or pure calculations.

Hooks may depend on React and pure libraries. Feature and app layers may depend on hooks.
